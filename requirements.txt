# §4.0 数据归一化模块（src/norm/）零第三方依赖，纯 Python 标准库即可运行。
# 下面均为可选依赖，按需安装：

# 可选：head 单复数归一质量提升（未安装时用内置规则兜底，质量略降）
nltk>=3.8

# §4.2 BM25 检索（运行时硬依赖）
rank-bm25>=0.2
numpy>=1.26

# 后续可选增强：
# rapidfuzz>=3.0        # location 模糊匹配（当前用受控词表替代，暂不需要）
# pydantic>=2.0         # schema 约束（已安装）
# sentence-transformers # §4.2 神经重排
